{
  // Development overrides — these values are ONLY active when
  // ASPNETCORE_ENVIRONMENT=Development (the default for `dotnet run`).
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information",
      "DiscordKeyBot": "Debug"
    }
  },

  // Set GuildId for instant slash command registration during local testing.
  // Replace with your test server's guild ID (right-click server icon in Discord).
  "Bot": {
    "GuildId": null
  }
}
